---js
{
title: "The Philosophy of Shakespeare"
layout: "layouts/default.njk"
footer: "footer1"
pagination:
    data: "collections.posts"
    size: 5
    reverse: false
    alias: "posts"
    before: function(paginationData, fullData) {
      // `fullData` is new in v1.0.1 and contains the full Data Cascade thus far

      return paginationData.sort((a, b) => {
        return new Date(b.date) - new Date(a.date);
      });
    }
}
---
<div class="w-100 overflow-hidden position-relative" id="top">
  {% include "snippets/hero2.njk" %}
</div>
<div class="container">
  <div class="row d-flex justify-content-center">
    <h2 class="col-11 col-lg-10 col-xl-6 text-center">Latest essays</h2>
  </div>
  <div class="row d-flex justify-content-center">
    <div class="col-11 col-lg-10 col-xl-6">
      <div class="row border-bottom">
        {% include "snippets/loop.njk" %}
      </div>
    </div>
  </div>
</div>
